# A node server that can run only with Docker

## Run the project with Docker:

Just run
```
docker-compose up
```
or
```
docker-compose up -d
```

and you should be able to code without having node, npm in your machine